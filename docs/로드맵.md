---
id: roadmap-0.4.7
date: 2026-08-17
tags: [roadmap, mcp, privacy, v0.4.0, v0.4.1, v0.4.2, v0.4.3, v0.4.4, v0.4.5, v0.4.6, v0.4.7]
---

# SnapContext 로드맵

> 대상 명세: [PRD 0.4.0](./PRD-0.4.0.md). 이 문서는 **실행 순서·완료 기준·에이전트 매핑**만 담는다(제품 스펙 중복 금지 — 티켓 상세·설계 근거는 PRD).

---

## 완료 트랙 (아카이브)

| 트랙 | 결과 |
|------|------|
| 0.2.x 잔여 (구 트랙 A) | ✅ 스위스 UI 이식·0.2.0 제출·changelog 정정 완료 |
| 0.3.0 MCP (구 트랙 B) | ✅ 2026-07-18 릴리즈 — `/mcp` 원격 서버 + 툴 3종(read-only), D1 인덱스, bearer(단일)·Origin 검증, E2E·태그 `v0.3.0`. 상세: [PRD 0.3.0](./PRD-0.3.0.md)·changelog |
| 0.3.0 스토어 제출 | ✅ **심사 통과** — Chrome 게시 2026-07-19 · Whale 승인 2026-07-21 (메일 실측 2026-07-27) |
| 0.3.x 품질 백로그 B1~B5 | ✅ PR #17 머지 (2026-07-18) |

---

## 트랙 D — 0.4.0 (본 트랙)

> 스코프·티켓 상세·DoD·리스크 = PRD 0.4.0이 SoT. 여기는 실행 순서와 게이트만.

한 줄: **per-user HMAC 토큰 멀티테넌시 + MCP 자발 사용(instructions) + 공유 만료 1/7/30일**.

| Phase | 내용 (티켓은 PRD) | 완료 기준 | 착수 조건 |
|-------|-------------------|-----------|-----------|
| P1 worker 토큰 기반 | token.ts(HMAC)·`POST /token`+rate-limit·D1 0002(owner) | 기존 테스트 무수정 그린 + test-d1 | — |
| P2 worker 인증 | /upload optional bearer·resolveMcpAuth(admin 우선→HMAC user)·history owner 필터 | 격리·admin 전체조회·malformed 401 테스트 그린 | P1 |
| P3 worker C5 만료 | readExpiry 헬퍼 전수 교체·expiresInDays allowlist·customMetadata·Cache-Control | 레거시 fallback 회귀 테스트 포함 그린 | P1 (P2와 독립 — 병렬 가능, 단 index.ts 충돌 주의 → worktree 분리) |
| P4 MCP 문구 | instructions·description·version 0.4.0 + 효능 스모크 | mcp-integration assert + Claude Code 실주입 기록 | P2 |
| P5 확장 | token 클라·upload bearer+expiresInDays·문구 동적화 | tsc strict + vitest + e2e upload-share | P2·P3 (worker 선배포 후) |
| P6 온보딩·문서 | 설정 패널·PRIVACY 개정·"7일" 문구 전수·ADR 011~013·register-mcp.ps1·스토어 킷 0.4.0 | e2e probe + 수동 QA 체크리스트 | P5 |

**운영 태스크 (전부 사람 게이트)**:

| 시점 | 태스크 |
|------|--------|
| P1 배포 전 | ✅ `TOKEN_SIGNING_SECRET` 주입됨 (`wrangler secret list` 실측 2026-07-27) |
| P3 배포 전 | ✅ R2 lifecycle **30일 상향됨** (rule명은 `auto-delete-7d`지만 action=30d — 실측 2026-07-27) |
| P1~P4 후 | ✅ worker 배포됨 — 라이브 serverInfo **0.4.1** (2026-07-26 01:53 KST 실측) |
| P6 후 | 0.3.0 심사 종료 ✅ → **0.4.0 스토어 제출 가능 상태** (사람 게이트, 미제출) |
| 각 PR | 머지 승인 |

**핸드오프 규칙 (0.3.0 교훈)**: Phase 완료마다 `docs/log/` 세션 로그 + Dev Log 행(산출물 포인터 필수). goal 실행 시 `.vhk/mission.json` 스코프 재설정(현재 0.3.0 스코프로 stale) — Phase별 scope/forbidden 고정.

---

## 트랙 E — 0.4.1~0.4.3 Private-by-Design

> 대상 명세: [PRD 0.4.2](./PRD-0.4.2.md)(P 핵심). 세대 계획 SoT = `~/.claude/plans/`. 여기는 실행 순서·게이트만.

한 줄: **"못 맞추는 링크" → 아이덴티티-프라이빗**(내 토큰의 AI만 내 캡처) + 온보딩 발견성 + 주석 확장. 근거: 현재 `snap_pack`/`snap_analyze` owner 무검사 + `/i`·`/s` 무인증 = id 가 곧 열쇠. 사용자 ~0인 지금이 모델 전환 최저비용.

| 버전 | 내용 | 배포면 | 재심사 | 완료 기준 | 착수 조건 |
|------|------|--------|--------|-----------|-----------|
| 0.4.1 | `/upload` rate-limit + ADR-014 2트랙 버전 + MCP이미지 호환 스파이크 | worker-only | ❌ | ✅ **완료·배포됨** (라이브 serverInfo 0.4.1, 2026-07-26) | — |
| 0.4.2 | **P**: owner-gate pack/analyze + C2 서명 `/i` + `/s` 2단계 제거 + **D0** 온보딩 메인승격 | worker+ext | ✅ #1 | PRD-0.4.2 DoD 7항 | ✅ plan-audit 승인(2026-07-27) — 완화: 0.4.0 심사통과=재심사#1 제출 전 조건·0.4.1 배포=1차 배포에 흡수 |
| 0.4.3 | **B**: 가리기(솔리드 박스·파괴적)/화살표/형광펜/자유선 + **A**: 토큰 regenerate-lite | ext-only | ❌(0.4.6 후 일괄) | PRD-0.4.3(approved 2026-08-16)·ADR-020·021 | 0.4.2 랜딩 ✅ |

**운영(사람 게이트)**: 0.4.1=`wrangler deploy`(재심사 없음) · 0.4.2=worker 1·2차 배포·PRIVACY 공개·tag `v0.4.2` · 0.4.3=tag `v0.4.3`. **스토어 재심사 제출은 1회 일괄**(요한 결정 2026-08-16 — 구 "재심사 #1·#2" 개별 제출 폐기. 제출 시점은 0.4.6 랜딩 후로 갱신, 2026-08-17 트랙 F 참조. 0.4.4의 "신확장 롤아웃 충분" 조건은 유저 ~0이라 요한 로컬 설치로 대체 판정 가능).

**★롤아웃 계약(ADR-015)**: `/s` 제거는 2단계 — **1차 배포=owner-gate만**(서명 `/i`·`{id,url}`→`{id}`·`/s` 제거 전부 2차 일괄 — PRD 결정#6, 2026-07-27 감사 확정), 신확장 스토어 롤아웃 충분 후 2차 배포. 라이브 0.4.0 확장(/s 링크 복사·`/s` 뷰어의 무서명 `/i` 임베드)이 심사 갭에 안 깨지게.

---

## 트랙 F — 0.4.4~0.4.7 부채 청산·다듬기 (2026-08-15 편성 · 2026-08-17 0.4.6 UX 삽입)

> 원칙: 전부 **이미 시작한 일의 마무리·다듬기**(신기능 아님) → patch 번호. 외부 사용자 유입(0.5.0) **전에** 보안 부채 0 + 첫 사용감 정돈 — 사용자 ~0인 지금이 최저비용. 배포는 한 번에 하나(문제 시 원인 특정·롤백 용이). 각 버전 착수 시 PRD 또는 ADR 1장으로 스코프 고정 후 진행.

| 버전 | 내용 | 배포면 | 재심사 | 완료 기준 | 착수 조건 |
|------|------|--------|--------|-----------|-----------|
| 0.4.4 | ADR-015 **2차 배포** — `/s` 제거·서명 `/i` 강제·`{id,url}`→`{id}` 일괄 | worker-only | ❌ | 무서명 `/i` 403·`/s` 410·구확장 잔존율 근거 기록 | 0.4.3 랜딩 + 신확장 롤아웃 충분(사람 판정) |
| 0.4.5 | rate-limit 승급 — `/token`·`/upload` per-isolate 임시 RL → **CF native rate-limit** (한도 설계 시 0.5.x 연속 캡처의 N장 연속 업로드 시나리오 고려 — 트랙 C) | worker-only | ❌ | 전역 한도 실측(다중 isolate에서 한도 공유 확인) + 기존 테스트 그린 | 0.4.4 랜딩 |
| 0.4.6 | **프롬프트 UX 다듬기** — ① payload 숨기기(요약 카드+복사 버튼, 원문은 접기) ② 프롬프트 다이어트(UA·뷰포트·좌표 = 버그 의도일 때만 포함) ③ 복사·저장 직후 다음 행동 안내 1줄 ④ 제품 용어 통일(캡처·팩·프롬프트 등 한 뜻 한 단어 — UI·문서·MCP 문구 전수) ⑤ 핀 의도 1비트(막힘/참고 토글 — ②의 포함 조건과 연동) ⑥ 업로드 상태 배지(전송됨·실패 — 조용한 실패 방지) (2026-08-17 요한 확정 — 0.5.x 후보에서 0.4.x로 승격, ④~⑥은 브레인스토밍 산물) | ext-only | ❌(일괄 제출에 편승) | 착수 시 PRD 1장 스코프 고정 · tsc+vitest+e2e 그린 · 수동 QA(첫 15초 흐름) | 0.4.3 랜딩 (worker 트랙 0.4.4~0.4.5와 독립 — worktree 분리 시 병행 가능) |
| 0.4.7 | 토큰 revoke 완성형 — D1 `owners` 발급 대장 + 완전 revoke·재발급 UI (0.4.3 regenerate-lite의 완성) | worker+ext | ✅ #3 | revoke 즉시 구토큰 401 + 격리 회귀 그린 | 0.4.5 랜딩 |

**운영(사람 게이트)**: 각 버전 `wrangler deploy` · 0.4.4=롤아웃 충분 판정 · 0.4.6=tag + **일괄 재심사 제출은 0.4.6 랜딩 후 실행**(0.4.2·0.4.3·0.4.6 ext 변경을 1회에 포함 — 추가 심사 사이클 0) · 0.4.7=D1 마이그레이션 승인·재심사 #3·tag. 0.4.x 완주 후 0.5.0(외부 사용자 dogfood·가치검증) 진입.

---

## 리서치 트랙 R — snap_capture 원격 캡처 (Web Push PoC)

> 0.4.0 구현 스코프 **밖** — 독립 리서치. 결과가 go면 0.5+ 트랙으로 승격.

- 배경: 0.3.0 Phase 0 결론 = 드랍 (원격 MCP→확장 즉시 깨우기의 검증된 패턴 부족, Web Push가 최유망하나 완결 E2E 공개 사례 미발견). 근거: `docs/research/phase0-capture-trigger.md`.
- **재검토 3조건** (phase0-capture-trigger.md:99 — 전부 충족해야 go):
  1. Web Push silent push + subscription 수명 관리 PoC 성공
  2. MCP 툴이 비동기 캡처 결과를 ≤N초 내 반환하는 프로토콜 확정
  3. 탭 선택·브라우저 오프라인 시 에러 시맨틱 ADR
- 산출물: `docs/research/webpush-capture-poc.md` + go/no-go 판정. PoC 코드는 별도 브랜치(머지 안 함).

---

## 트랙 C — 0.5+ 후보 (목록만)

> 2026-08-17 브레인스토밍(ADHD 5프레임×30안 → 심화 3안)으로 방향 가닥 — 확정은 각 버전 PRD에서.

- **0.5.0 가안: 텍스트 우선 컨텍스트 프로토콜** — 캡처 시점 DOM 스냅샷(핀 근처 요소 텍스트·rect) 수집 + MCP 2계층 응답(텍스트 digest 먼저·이미지는 서명 URL lazy) + 핀 반경 crop 옵션 + 토큰 실측 계측(D1) → "browser-use 대비 왕복당 N배 저렴"을 수치로 확보. ★DOM 스냅샷 파이프라인은 아래 Debug Pack·git-트랙·선제 핀의 공통 기반 공사.
- **0.5.x: git-트랙 캡처 자산** — `npx snapcontext pull` CLI가 owner 토큰으로 당겨 `.snapcontext/captures/*.json` 사이드카 저장(이미지는 R2 참조로 블로트 회피) → MCP 미설정 클라이언트·CI·타 머신도 파일 Read만으로 캡처 컨텍스트 소비.
- Debug Pack — `console.error`/network 에러 자동 수집(`debug` 필드). (0.5.0 캡처 파이프라인과 동일 기반)
- before/after 비교 캡처(`comparison` 필드).
- **0.5.x 확정: 연속 캡처(멀티 프레임 팩)** — 사람이 슬라이드·탭을 넘기면 클릭·키 이벤트마다 자동 촬영(Scribe 방식·MutationObserver 아님), N장이 순서 있는 한 팩(캐러셀·쇼핑 상세·온보딩). 직추출 가속(인스타 등)은 후속 옵션. before/after·합짐배송과 "팩 다중 이미지" 스키마 확장을 한 세트로 설계. 로컬엔 썸네일만·원본은 서버(storage budget 패턴). (2026-08-17 요한 go 결정 — 근거: Scribe/Tango 선례 + "AI 컨텍스트용 멀티 프레임" 경쟁 갭. 착수 시 스키마 변경 = L 라우팅·PRD 필수)
- `vhk recap` 연동 확장 — Context Pack 자동 수집.
- **0.6 후보: 선제 핀(스펙 핀)** — 핀 시제 역전(`intent: report|spec`), 요구→구현→재캡처 핀 단위 검증 루프. 화면으로 확인 불가한 요구의 판정 신뢰성 리스크로 0.5 이후 보류.
- ~~화살표·블러·형광펜 주석~~ → **0.4.3으로 승격** (트랙 E).
- ~~토큰 revoke·재생성 UI (0.4.0 비목표에서 이월)~~ → **0.4.7로 승격** (트랙 F).
- ~~프롬프트 UX 다듬기·용어 통일·핀 온도계·전달 영수증~~ → **0.4.6으로 승격** (트랙 F).

---

## 에이전트 역할 매핑

> SoT = `yohan-brain/memory/core/agent-roster.yaml` (실제 모델·티어는 그 파일이 결정 — 여기 복제 금지). 전 Phase 일괄 최상위 티어 상시 할당 금지.

| lifecycle | 역할군 | 적용 |
|-----------|--------|------|
| implement | balanced_or_strong | P1~P6 구현 |
| verify | balanced_or_strong (기본) → 최종 릴리즈 verify만 strong 상향 | 각 Phase 검증 |
| review | strong_or_conductor | P2 인증·격리, P6 PRIVACY 정합 — 리뷰 필수 |
| research | cheap_or_balanced | 트랙 R PoC 조사 |
| release | conductor + **사람 게이트(secret·lifecycle·deploy·스토어·태그·머지)** | 운영 태스크 전부 |
