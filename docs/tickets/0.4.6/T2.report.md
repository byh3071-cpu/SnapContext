[결과] 성공 — 기본 프롬프트는 URL+핀 메모+요청 1줄, 환경·좌표는 버그 템플릿+버그 핀일 때만
[변경] src/context-pack/prompt-builder.ts: ctx.debug=bug템플릿&&hasBugPin · lite=!debug · 핀 tag · UA/뷰포트/캡처방식은 debug일 때만 채움
[변경] src/context-pack/template-engine.ts: 중첩 {{#if}}가 안쪽부터 닫히도록 최소 수정(테스트 동반)
[변경] prompts/templates/bug-report.md: 다이어트 골격 · 핀 메모 선행 · 요청 1줄 · 환경은 debug · 용어 통일 · date 2026-08-29
[변경] prompts/templates/refactor.md: 동일 골격 · 요청 1줄(개선안 코드) · 환경 절은 debug 전용(refactor에선 항상 생략)
[변경] prompts/templates/reference.md: 동일 골격 + 페이지 제목 · 요청 1줄(구조 분석·구현)
[변경] tests/context-pack.test.ts: 기존 buildTemplatePrompt 검증 갱신 + 조건부 렌더·중첩 if/each 신규
[검증] pnpm test → 20 files / 156 passed · pnpm build → version-sync OK 0.4.3 · tsc 0 error · vite build OK (730ms)
[테스트] includes UA, viewport, and pin coords when bug template has a bug pin / omits UA, viewport, and coords when bug template has only ref pins / omits UA and viewport on refactor/reference even with a bug pin / places the pin memo section before the request section / has no 4-item numbered instructions and no forbidden glossary terms / renders {{#each}} inside {{#if}} inside {{#if}} · 먼저 실패 확인 예 (구현 전 context-pack 5 failed | 7 passed)
[스코프 밖 발견] ContextPackPanel.ts · types/index.ts · pin-kind.ts · generator.ts · worker/** · docs T4b — 손대지 않음. template-engine.ts는 티켓 허용(중첩 if 최소 수정).
[가정·미해결] 핀 tag는 선행 공백 `' [버그]'`로 렌더가 `**핀 1** [버그]:`가 되게 함(티켓의 `'[버그] '` 리터럴은 콜론 앞 공백이 어색). 독푸딩 CIM 테스트는 전체 병렬 시 5s 타임아웃이 날 수 있음(재실행·최종 pnpm test는 156 passed) — T2 무관.
[다음] 없음

## 렌더 예시 1 — 참고 핀만 (bug 템플릿, kind 없음)

```
# 🐛 버그 리포트
- URL: https://example.com/page
## 핀 메모
- **핀 1**: look here

## 요청
핀 메모의 문제 원인과 수정안을 제안해주세요


[첨부: 캡처 이미지]
```

## 렌더 예시 2 — 버그 핀 포함 (bug 템플릿, kind:'bug')

```
# 🐛 버그 리포트
- URL: https://example.com/page
## 핀 메모

- **핀 1** [버그] (10.3%, 20.5%): button is broken
## 요청
핀 메모의 문제 원인과 수정안을 제안해주세요

## 환경
- 뷰포트: 1280×720 · UA: Test UA · 캡처 방식: visible
[첨부: 캡처 이미지]
```
